<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
    </head>
    <body>

    {% if result %}
    The timezone {{ result.inputTimezone }} has {% if result.offsetToUTC > 0 %}+{% endif %}{{ result.offsetToUTC }} minutes offset to UTC.<br />
    February in this year has {{ result.daysInFebruary }} days.<br />
    Specified month ({{ result.inputMonth }}) has {{ result.daysInInputMonth }} days.
    {% endif %}

    {{ form(form) }}
    </body>
</html>
